# PostgreSQL C2 real clean-room readback (PG-KS-03)

The AC02/AC03 real positive/negative clean-room for the PostgreSQL C2 safe-aggregate capability was executed on 2026-09-12 in the Qwen dedicated test VM under the recorded KS150-scoped authorization (grant `EXECUTION_AUTHORIZED_DEPENDENCIES_REMAIN_BINDING`, recorded 2026-09-12T19:42:10.963372+00:00). The substrate was a newly created disposable, loopback-only PostgreSQL (transport `127.0.0.1:5432`, database `ks_fixture`, SCRAM-SHA-256) fed only the 17-row synthetic non-customer net-revenue holdout; the read path ran as the least-privilege read-only principal `kaleidosphere_read_only_analyzer` through the file-secret route. The full machine record of the run is committed byte-for-byte at `.ks150-c2-real-cleanroom-primary-evidence.json` and `.ks150-c2-real-cleanroom-post-restore-evidence.json` — both are byte-identical (sha256 `b3c10b112edf72bbf6241691d686cc2adc7e4380e3a9a238618ac0f3dd9ca382`) because the post-restore real read is byte-reproducible — and is bound by the provenance record `verification/postgresql/postgresql-c2-real-cleanroom-provenance-v1.json`, whose gate test pins the raw digests and the tested source head `28b50870d2ab360ebce76d524ab2636254382c22` and verifies that no product, config, fixture, contract, certificate, profile, or C1 byte changed after that head.

AC02: the regular product path ran LIVE (`productPath.mode = LIVE`, dispatch REGULAR, runtimeValidation RUNTIME_VALIDATED; descriptor route `postgresql.run-queries` / `postgresql.read-only-session` session / `preflight.coverage-ledger` evidence) for the typed operation `bi-ks-01-net-revenue/v1`. The real bounded read (17 rows, row budget 17, byte budget 4096, timeout 100 ms, `BEGIN READ ONLY`) re-serialized to exactly the admitted holdout digest `2d0ba0bb806e73a473688d6137c6182f4233aec1bed92aee708c4a052d327a4d`, the receipt state was `COMPLETE` with `accessMode READ_ONLY`, `mutationAuthority false`, `readOnlyEvidence VERIFIED`, `bounded`, `timeoutAware`, and `cancelAware` all true, and the typed-plan digest binding held (plan `90bca7ef18339928f1dd70bcfc5288e853045bd5f08776c700a7c863ec4526a8`, metric contract `455f735e55f03155c657dc963656ed01363e546345824dfea66b883c287d9d70`, oracle `ce0c135351a8179f08cfca77b91a9624f2f6a7e16fd81cda8c3aba780f4a9164`). The session proof recorded `transactionReadOnly on`, `defaultTransactionReadOnly on`, `adminCapabilities false`, and the frozen-substrate re-assertion verified `c1ProfileMatchesFrozen true` and `c1CertificateMatchesFrozen true`.

AC03: the real-read net-revenue result equalled the independent BI oracle exactly (`oracleEquality EXACT`; result digest `ab756c46131ab8d1be491b5ef7f082587fc1475a7fff4f20443966e97cbe02fc`, output digest `6e92091e32b348c3db91ae18fe43e485c653f696a21938c0a77379e55ada6ae2`; `current.netMinorUnits 100059`, `comparison.netMinorUnits 30000`, `deltaMinorUnits 70059`, `excludedOutOfScopeCount 3`). All seven fail-closed probes fired to their distinct truthful denials — `DB_ANALYZE_CREDENTIAL_MISSING`, `BUSINESS_BI_OPERATION_DENIED`, `DB_ANALYZE_SECRET_BINDING_MISMATCH`, `DB_ANALYZE_DESCRIPTOR_STALE`, `BUSINESS_BI_SOURCE_SCOPE_DENIED`, `BUSINESS_BI_READ_ONLY_EVIDENCE_DENIED`, and `DB_ANALYZE_PRINCIPAL_NOT_READ_ONLY` — and the committed `verification/postgresql-c2-safe-aggregate-v1.json` (sha256 `630096d44765665b6aa13d6d99f897c80dc2b011e6cfe9cd7efd027a25147e3b`) stays the byte-deterministic source-local mint it always was; this real-run evidence is registered as separate later provenance and never re-mints or relabels the certificate or any frozen C1 byte.

Recovery: the relation was dumped with `pg_dump --no-owner --no-privileges -t synthetic_bi.orders` (sha256 prefix `8b7de3b6`), dropped and re-applied; the row count was 17 before and after the restore and the re-run real read reproduced `COMPLETE` / `EXACT` byte-identically. The read-only principal's `UPDATE` was denied in-transaction and the row count stayed 17.

Teardown was verified to zero residue: the container, its exact-digest image (`sha256:94f23d40…`) and data volume (`sha256:8619b8bc…`) were removed and all transient artifacts were deleted; the resident `postgres:16` image and pre-existing volume in the VM were left untouched.

No credential, connection string, or raw row value is reproduced here.
